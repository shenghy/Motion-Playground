# Overlay Studio 本地启动说明

Overlay Studio 继续以普通本地网页运行，不需要安装 Electron、Tauri 等桌面框架。

## Windows

直接双击项目根目录中的：

`启动 Overlay Studio.bat`

也可以双击桌面上的 `Overlay Studio` 快捷方式。

## macOS

双击：

`启动 Overlay Studio.command`

如果 macOS 首次阻止运行，可以在终端进入项目目录执行：

```bash
chmod +x "启动 Overlay Studio.command"
./启动\ Overlay\ Studio.command
```

## 启动过程

1. 首次启动且依赖不存在时，脚本自动执行 `npm install`。
2. 每次启动自动执行生产构建，确保运行当前源代码。
3. 默认从 `http://127.0.0.1:4173/` 开始寻找可用端口。
4. 如果端口被占用，会自动尝试后续端口。
5. 服务就绪后自动打开系统默认浏览器。

启动窗口需要保持打开。关闭窗口或按 `Ctrl+C` 会停止本地服务。

## 命令行启动

也可以在项目目录运行：

```bash
npm run start:local
```

只启动服务但不自动打开浏览器：

```bash
set OVERLAY_STUDIO_NO_OPEN=1
npm run start:local
```

macOS 使用：

```bash
OVERLAY_STUDIO_NO_OPEN=1 npm run start:local
```

## 运行要求

- 已安装 Node.js LTS
- `node` 与 `npm` 命令可以在终端中使用
- 本地端口 `4173` 至 `4192` 至少有一个可用

如果构建或依赖安装失败，启动窗口会保留错误信息。根据提示处理后重新双击启动即可。

## 导出透明动效层

1. 导入视频并在时间轴添加卡片。
2. 在右侧“透明动效层”中选择导出方式。
3. “导出透明 MOV”会生成可直接叠加到剪映、Premiere Pro 或 Final Cut Pro 的 ProRes 4444 Alpha 文件。
4. “导出 PNG 序列”会创建带时间戳的文件夹，并按 `frame_000001.png` 的格式逐帧保存。

两种格式均为 `1920×1080 / 30帧`，覆盖导入视频的完整时长，只包含动效卡片，不包含原视频画面。导出过程中可以取消；PNG 已完成的帧会保留，MOV 保存失败后可再次点击按钮重试保存。

透明 MOV 依赖启动脚本提供的本地编码服务，因此请通过 `启动 Overlay Studio.bat`、桌面快捷方式或 `npm run start:local` 打开。推荐使用最新版 Chrome 或 Edge，以便网页选择保存文件和文件夹。
